@model asiakasrekisteri.Models.Asiakasluokittelu

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Lisätiedot</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Toimiala)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Toimiala)
        </dd>

        <dt>
            @Html.DisplayNameFor(Model => Model.Liitto)
        </dt>

        <dd>
            @Html.DisplayFor(Model => Model.Liitto)
        </dd>

    </dl>
</div>
<hr />
        
<div>
    <a type="Button" class="btn btn-info btn-sm" href="@Url.Action("Edit", "Asiakasluokittelu", new {id = Model.LuokitteluID })">Muokkaa</a>
    <a type="Button" class="btn btn-danger btn-sm" href="@Url.Action("Delete", "Asiaksluokittelu", new {id = Model.LuokitteluID })">Poista</a>
    <a type="Button" class="btn btn-info btn-sm" href="@Url.Action("Index", "Asiaksluokittelu", new {id = Model.LuokitteluID })">Takaisin</a>
</div>

