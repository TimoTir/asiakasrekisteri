@model IEnumerable<asiakasrekisteri.Models.Asiakasluokittelu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Toimiala)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Liitto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Toimiala)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Liitto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LuokitteluID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LuokitteluID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LuokitteluID })
        </td>
    </tr>
}

</table>
